# Ensemble launches workspace commands from the packaged core process on
# Windows. That security context rejects pnpm's package junctions as
# "untrusted mount point", even though the store target exists. A hoisted
# layout materializes resolvable package directories/hardlinks instead of
# requiring runtime traversal through those junctions.
node-linker=hoisted
inject-workspace-packages=true
